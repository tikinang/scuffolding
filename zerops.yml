web:
  build:
    base:
      - go@1
    build:
      - export # FIXME: mpavlicek - remove me
      - git describe --tags --abbrev=0 # FIXME: mpavlicek - remove me
      - go build -o icbaat-web -v -ldflags="-X 'main.version=v1.0.0-alpha'" cmd/web/main.go
    deploy:
      - icbaat-web
  run:
    start: ./icbaat-web run
